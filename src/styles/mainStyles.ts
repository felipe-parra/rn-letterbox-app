import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  view: {
    fontSize: 16,
    fontWeight: 'bold',
    marginBottom: 8,
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  h1Title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 8,
    color: 'teal',
  },
  buttonPrimary: {
    backgroundColor: 'red',
    color: 'green',
    padding: 8,
    borderRadius: 4,
    borderColor: '#000',
    borderWidth: 1,
  },
});
